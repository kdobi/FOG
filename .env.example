# 로컬 개발에서는 비워두면 Vite가 /api 요청을 내부 서버로 전달합니다.
# 웹과 API를 별도 도메인으로 배포할 때만 실제 API 주소를 입력합니다.
VITE_API_BASE_URL=

# 기상청 API허브에서 발급받은 인증키
# 실제 값은 .env.server에 저장하고 GitHub에 올리지 않습니다.
KMA_AUTH_KEY=
API_PORT=8080

